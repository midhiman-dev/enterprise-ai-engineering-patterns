# Utility Scripts

This directory is reserved for future utility scripts (e.g., knowledge base ingestion, chunking benchmarks, evaluation runners).

## Pass-0 Status

No executable scripts are created in Pass-0. Scripts will be introduced in subsequent passes alongside their corresponding behavior.
